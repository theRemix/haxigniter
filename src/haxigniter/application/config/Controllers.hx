package haxigniter.application.config;

/**
* In this file, import all controllers that your application uses. For example:
*
* import haxigniter.application.controllers.start;
*
* This is because dynamic loading of classes in haXe isn't possible, so they
* have to be defined somewhere to be referenced by the compiler. Sorry for the 
* incovenience.
*
*/
import haxigniter.application.controllers.Start;
