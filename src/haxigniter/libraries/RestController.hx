package haxigniter.libraries;

import haxigniter.libraries.Controller;

/**
 * Use this controller instead of Controller to get the Ruby on Rails RESTful approach for a controller. Recommended!
 */
class RestController extends Controller {}
